apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-productorderingapi
  labels:
    app: {{.Release.Name}}-{{.Values.component.name}}
    oda.tmforum.org/componentName: {{.Release.Name}}-{{.Values.component.name}}
spec:
  ports:
  - port: 8080
    targetPort: {{.Release.Name}}-poapi
    name: http-{{.Release.Name}}-productorderingapi
  type: NodePort
  selector:
    impl: {{.Release.Name}}-productorderingapi
